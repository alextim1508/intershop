app:
  partition-count: 4

spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: never

hibernate:
  dialect: org.hibernate.dialect.H2Dialect

